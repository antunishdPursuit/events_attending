import UserDetails from "../Components/UserDetails";

function Show() {
    return (
        <div className="container">
            <br></br>
            <UserDetails />
        </div>
    );
}

export default Show;
